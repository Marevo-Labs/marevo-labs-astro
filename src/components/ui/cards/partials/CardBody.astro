---
// Card body
// ------------
// Description: the body of the main content of the card
// Properties:
// - classes: extra classes

// Props
type Props = {
  classes?: string
}
const { classes } = Astro.props
---

<div class:list={["p-6 space-y-4", { [`${classes}`]: classes }]}>
  <slot />
</div>
