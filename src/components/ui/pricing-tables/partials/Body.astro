---
// Pricing body
// ------------
// Description:  The body of the pricing table
// Properties:
// - classes: Additional classes to be added to the text field

// Props
type Props = {
  classes?: string
}
const { classes } = Astro.props
---

<div class:list={["pricing__body p-6", { [`${classes}`]: classes }]}>
  <slot />
</div>
